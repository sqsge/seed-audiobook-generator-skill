interface:
  display_name: "Seed Audio Drama Generator"
  short_description: "Build resumable long-form audio dramas with pilot approval"
  default_prompt: "Use $seed-audiobook-generator to plan, validate, pilot, approve, generate, replan, resume, or inspect a long-form Seed Audio drama while preserving every artifact."
